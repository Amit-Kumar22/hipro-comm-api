# Development Environment Variables for API

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/hipro-comm-dev

# JWT Configuration
JWT_SECRET=development-jwt-secret-not-for-production
JWT_EXPIRES_IN=7d
COOKIE_SECRET=development-cookie-secret-not-for-production

# CORS Configuration
FRONTEND_URL=http://localhost:3201
ADMIN_URL=http://localhost:3202

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Upload Configuration
UPLOAD_PATH=uploads
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif

# Logging Configuration
LOG_LEVEL=debug

# Email Configuration - Hostinger SMTP (same for dev/prod)
EMAIL_SERVICE=smtp
EMAIL_USER=info@hiprotech.org
EMAIL_PASS=Abhi@2026
EMAIL_FROM=info@hiprotech.org

# Hostinger SMTP Settings
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@hiprotech.org
SMTP_PASS=Abhi@2026
SMTP_FROM_NAME=HiPro Commerce (Dev)
SMTP_FROM_EMAIL=info@hiprotech.org